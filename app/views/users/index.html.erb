<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<%= turbo_frame_tag "paginate_page_#{@users.current_page}", target: "_top", data: { turbo: true } do %>
  <div class="row">
      <%= render @users %>
  </div>

  <% if @users.next_page %>
    <%= turbo_frame_tag "paginate_page_#{@users.next_page}", src: users_path(page: @users.next_page), loading: "lazy" do %>
      Loading...
    <% end %>
  <% end %>
<% end %>
